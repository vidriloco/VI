<%= element_view_for(element) do |el| -%>
	<p class="details">
		<%= el.render :address %>
		<br/>
		<%= el.render :phone %> 
	</p>
	<p class="contact-email">
		<%= mail_to el.render(:email), "Contáctanos" %>
	</p>
<%- end -%>
